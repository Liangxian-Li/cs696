This is a README file for Lab3.



\## Environment Setup



This project uses environment variables stored in a `.env` file.



Before running the project with Docker Compose:



1\. Copy `.env.example` to `.env`

2\. Update the variable values if needed

3\. Run:



```bash

docker compose up --build -d

